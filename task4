# Створення оновленої копії запису (Haskell)
# Створіть тип Person, що містить ім’я та вік. Реалізуйте функцію incrementAge, яка повертає новий екземпляр з віком, 
# збільшеним на 1. Початковий запис не змінюється.
# data Person = Person { name :: String, age :: Int } deriving Show 
# incrementAge :: Person -> Person

main :: IO ()
main = do
    putStrLn "Введіть ім'я:"
    n <- getLine

    putStrLn "Введіть вік:"
    a <- readLn

    let p  = Person n a
    let p2 = incrementAge p

    putStrLn ("Нова людина: " ++ show p2)
    putStrLn ("Стара людина: " ++ show p)
